import React from "react";

const JavaScript = () => {

    return( 
        <div>
            <h4>JavaScript Project</h4>
            <p>In the JavaScript block we achieved this in a group project. Learned a lot about balancing workload with other co-workers and how to support each in the development.</p>
        </div>
    )
};

export default JavaScript;